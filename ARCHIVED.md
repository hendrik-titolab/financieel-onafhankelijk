# Deze branch is gearchiveerd

**`main` is sinds 10 augustus 2026 niet meer de live site.** De cutover naar de
Astro-herbouw is toen uitgevoerd; `benikfinancieelonafhankelijk.nl` draait sindsdien
op branch **`astro-migratie`**, en die is ook de Vercel Production Branch.

Deze branch (de oude React/Vite-SPA) blijft staan als referentie en noodgreep-
rollback, maar krijgt geen nieuw werk meer. Nieuwe features, content en fixes horen
op `astro-migratie`.

Zie `HANDOFF-app-installatie.md` op `astro-migratie` voor de volledige
overdracht van de cutover.
